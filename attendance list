#include <iostream>
#include <fstream>
#include <vector>
#include <string>

using namespace std;

int main() {
cout << "-------------------\n";
cout << "[ Attendance List ]\n";
cout << "-------------------\n";

vector<string> people = {
"exampleOne",
"exampleTwo",
"exampleThree",
"exampleFour",
"exampleFive",
"exampleSix",
"exampleSeven",
"exampleEight",
"exampleNine",
"exampleTen"
};

string date;

cout << "What is the date today (MM-DD-YY): ";
cin >> date;

// Create attendance file
ofstream attendanceFile(date + ".txt");

if (!attendanceFile) {
cout << "Error creating file.\n";
return 1;
}

attendanceFile << "Attendance for " << date << "\n";
attendanceFile << "-------------------\n";

for (string person : people) {
char present;

cout << person << " present? (Y/N): ";
cin >> present;

attendanceFile << person << " - ";

if (present == 'Y' || present == 'y') {
attendanceFile << "Present\n";
} else {
attendanceFile << "Absent\n";
}
}

attendanceFile.close();

cout << "\nAttendance saved to " << date << ".txt\n";

return 0;
}
